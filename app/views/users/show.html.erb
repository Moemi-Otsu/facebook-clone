<div class="new-edit-box user-box">
  <h1>プロフィール</h1>
  <h2><%= @user.name %>さんのページ</h2>
  <p>メールアドレス: <%= @user.email %></p>
  <% if logged_in? %>
  <p class="btn-login"><%= link_to '投稿を見る', blogs_path %></p>
  <% else %>
  <p class="btn-login"><%= link_to 'ログインする', new_session_path %></p>
  <% end %>
</div>