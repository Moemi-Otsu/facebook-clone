<h1>ブログ一覧</h1>
<p><%= notice %></p>
<div class="container">
<% @blogs.each do |blog| %>
  <div class="row">
    <p><%= blog.content %></p>
    <p><%= link_to '詳細を確認する', blog_path(blog.id) %>
    <p><%= link_to '編集する', edit_blog_path(blog.id) %></p>
    <p><%= link_to '削除', blog_path(blog.id), method: :delete, data: {confirm:'本当に削除していいですか？'} %></p>
  <div>
<% end %>
<div>

<%= link_to '新規作成', new_blog_path %>
<%= link_to 'ブログ一覧画面にもどる', blogs_path %>